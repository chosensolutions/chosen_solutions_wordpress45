let bookModel = require('../book.model');

describe('Test Suite: Models', () => {

  it('Model - Book', () => {
    // 1. Arrange


    // 2. Act


    // 3. Assert
    // expect(registerUserRequest).toEqual({ email: undefined, password: undefined });
  });
});